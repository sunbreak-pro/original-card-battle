# 踏破者（player_swordsman）— 仕様カード

- 作成日: 2026-09-20 / 工程: C0（`visual-production-pipeline character player_swordsman`）
- 正本: `.claude/docs/vision/concept-v3.md` §16（襲名と装束）/ §1 / §14 / `.claude/docs/battle_document/battle_ui_ux_v2.md:1202-1206` / `unity-port/unity-project-kit/Assets/View/ArenaView.cs`
- 手順書: `docs/reports/2026-09-19-krita-ai-setup-guide.html` 4 章

## 1. 誰か

「踏破者」はダンジョンへ潜る**役目**の名です（`concept-v3.md` §16.1）。装束・面・剣は役の支給品で、人が替わるたびに返却と再支給を繰り返します。**名前は個人のもの、姿は役のもの**。世代ごとに中身は別人で名前も違いますが、立ち絵は 1 体で足ります。

絵で伝えるべきことは 2 つです。**顔が見えないこと**と、**支給品を着ていること**。個性を出さないことが設計です。表情で感情を語れない代わりに、面の角度と肩・手・マントの動きで語ります。

**絵柄**: 線・塗り・色・光は `art_document/style-guide.md` の第 2 部（§12〜§19）に従います。装束は無彩か寒色の低彩度にし、暖色は灯りと留め具だけに集めます（同 §14.1）。影絵は竜人と並べて区別できる外周（面・フード・マント）にします（同 §16.1・§17）。

## 2. 画面での大きさと基準点

| 項目         | 値                           | 出典                     |
| ------------ | ---------------------------- | ------------------------ |
| 表示枠       | 140 × 300 px（比 約 1:2.14） | `ArenaView.cs:35-36`     |
| 基準点       | 足元の中央（Root pivot）     | `ArenaView.cs:12`        |
| 左右の向き   | 右向きで描く。プレイヤーは反転しない（`Sign = -1` → `scaleX = 1`）。画面左に立ち、右の敵を向く | `ArenaView.cs:26-28` |
| 胸の基準     | 数字は足元から `300 × 0.72 = 216 px` に湧く | `ArenaView.cs:489` |
| 原本の大きさ | 1024 × 1536 の文書。取り込みは長辺 1024 | 手順書 段階 0 |

**輪郭の制約**: フードとマントは横幅を食います。枠幅 140 px（C1 の関門は 130 px 以内）に収めるため、マントは背中側へ流し、左右へは広げません。残像（ムーブ）と `StanceGlow` は輪郭の形をそのまま使うので、**シルエットが人型と読めること**を優先します。

## 3. 装束（`concept-v3.md` §16.3 が正）

| 部位     | 仕様                                                     | 描くときの注意                                     |
| -------- | -------------------------------------------------------- | -------------------------------------------------- |
| 面       | 顔全体を覆う。目の位置は細い切れ込みか覆い。表情は出ない | 目を光らせない（敵の歪みと読み違える）             |
| フード   | 面の上から被る。面とフードで頭部の輪郭を固定する         | 頭部が枠の上端から 20 px 以上下がること（予兆バナーは敵側だが、被弾の跳ねで頭が出る） |
| マント   | 背中から。歩行・被弾・構えの揺れを担う                   | 背中側へ流す。左右へ広げない                       |
| 服       | 動きやすさを優先した長衣と脚衣。装甲は置かない           | アーマーは凍結中（§3.4）。板金・肩当てを描かない   |
| 剣       | 支給品。刃の意匠は代を通じて同じ                         | 片手剣。全長は身長の 0.45 以内（枠に収める）       |

**世代差は支給品の意匠だけに出す**。前の生の到達階層で階級が上がり、面の縁飾り・マントの裏地の色・留め具の意匠が変わります。**輪郭（面 + フード + マント）は変えません**。差分はテクスチャの差し替えで作り、モデルは 1 つです。

## 4. 差分の枚数

`battle_ui_ux_v2.md:1206` のプレイヤー行がそのまま適用されます。

| 区分       | 内容                                                  | 枚数 |
| ---------- | ----------------------------------------------------- | ---- |
| アタック   | 斬 / 突 / 払 / 打                                     | 4    |
| ガード     | 剣で受ける                                            | 1    |
| スタンス   | 構え                                                  | 1    |
| スキル     | 固有                                                  | 1    |
| 息         | 呼吸                                                  | 1    |
| ムーブ     | 残像と位置補間で作る（差分なし）                      | 0    |
| 被弾 / 崩れ / 倒れ |                                               | 3    |
| 体勢       | lean で畳む                                           | 3→1  |

**C3 で作るのは待機 1 枚だけ**です。ファイル名は `Assets/Art/Characters/player_swordsman/chr_player_swordsman_<category>_<label>.png`、待機は `chr.player_swordsman.idle.stand`。

**要判断**: `battle_ui_ux_v2.md:1202` はプレイヤーの Live2D パラメータを lean + 目 + 口 の 3 としています。面で顔が隠れるので 目・口 は使えません。lean 1 に減らすか、面の角度を別パラメータに立てるかを決める必要があります（`concept-v3.md` §16.5-3）。

## 5. 生成の指示文（段階 1・案出し用）

スタイルは `card-battle Animagine`、文書は 1024×1536、生成回数 4 を 2〜4 回まわして 8〜16 枚出します。

```
1other, solo, full body, standing, from side, facing right, full face mask, faceless, hood over mask, long cloak behind back, layered robe, holding one-handed sword lowered, no armor, no shoulder plates, japanese dark fantasy, muted colors, simple background, grey background
```

禁止事項: 既存の作品名・作者名を入れない。流血を描かない。購入素材や他作品の画像を参照に入れない。素肌と表情を出さない。

## 6. 進捗

| 工程 | 状態   | 備考                                                             |
| ---- | ------ | ---------------------------------------------------------------- |
| C0   | 下書き | このファイル。こうだいさんの承認待ち                             |
| C1   | 未着手 | 影絵 SVG。フードとマントの横幅 130 px の判断はここで決める       |
| C2   | 一部   | 指示文は §5 にある。台帳の行はまだ（`Docs/art/asset-ledger.csv` が未設置） |
| C3   | 人待ち | Krita で 8〜16 枚。指示文は §5 をそのまま貼る                    |

**次に人がすること**: Krita で候補を出し、2〜3 枚を Claude に渡す。

**この決定で解けた前提**: `art_document/style-guide.md` は「主人公の見た目が未決」で止まっていました（`briefs/polearm_warped.md` 末尾）。装束が決まったので setup S0/S1 に進めます。
