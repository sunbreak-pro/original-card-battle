# Tier 1 — コア要件（これが無いと「リアル性 v2」が成立しない）

> **Status**: DRAFT v2（クリティーク3観点 FAIL 指摘を反映。再検証待ち）
> **Created**: 2026-06-11 / **Revised**: 2026-06-13
> **入力**: `docs/vision/concept-v2.md`（APPROVED）/ `docs/vision/2026-06-11-gap-analysis.md`（§3 横断 X-1〜X-8 / §4 修正15件 / §5 クリティカルパス / §6 決定7件）
> **原則**: 数値・仕様の正は設計書（`docs/*_document/`）。本書は「何を・なぜ・どの順で」を定義し、数値は設計書に委譲する。
> **合格基準（concept §5）**: (1) 設計書と実装の差分ゼロ (2) 2ステージ通しプレイで破綻なし

## 表記

- **由来**: concept-v2 の節 / gap-analysis のギャップ ID / §6 決定番号
- **依存**: 先行して完了すべき要件 ID（gap-analysis §5 クリティカルパス準拠）
- **受け入れ基準**: 観測可能な pass/fail 条件

## v2 改訂サマリ（クリティーク反映点）

- TD-1（known-issue 001）を Phase 0 へ復帰（concept §5「着手前に全返済」準拠。先送りは正本逸脱だった）。TD-3/TD-5/TD-7/TD-6 も R1-0 に追加
- 剣士カード枚数を 40 → **全60枚（基本37 + タレント8 + 派生15、CRD-3 実測）** に訂正
- 学習手段（X-4）を Tier 2 から **Tier 1（R1-17）** へ移設（合格基準2「詰みなし」の前提のため）
- 副柱2「不可逆性」の実行系を **R1-16** として新設（IRR-1/3/4/6/8/12）
- BAT-12（オーケストレーター合成境界整理）を R1-2.5 として明示。BAT-7（剣気再定義）/ BAT-10+DOC-4（バフ42種棚卸し）を R1-1/R1-2/R1-4 に編入
- CRD-4（移動・体勢カード新設）/ CRD-5（初期デッキ再構成）/ CRD-8（熟練度ボーナス再定義）を R1-7 に編入
- DUN-10（帰還システム）を R1-18（前半）/ Tier 2（後半）に分割
- 領域別の先行設計書（DOC-1/9/10/13/15/16）を各実装要件の前段として明記。設計書駆動を構造化
- R1-5 の敵母集合を「全45体」→「2ステージ配属敵 + 試験敵5体（未配属は凍結保持）」に縮小（ENM-9/DOC-13 準拠）

---

## R1-0: Phase 0 負債返済（着手前・concept §5）

- **由来**: concept §5「着手前に全返済」/ gap-analysis §5 Phase 0・X-8 / TD-0〜TD-7
- **内容**:
  - (a) ~~TD-0: main コンパイル不能解消 + .gitignore 復元 + Python 教材削除~~ → **PR #13 で完了**
  - (b) TD-1: 共鳴デバフの play-aware 化（`getResonanceEffectsForPlay(card)` を `useElementalChain` に追加し `onCardPlay` 後の仮想 state から導出）+ 回帰テスト。純関数追加中心・工数 S。新コアのスタミナ/体勢 state でも同型の stale-state 対称化が要るため、その定石をテスト付きで先に確立する
  - (c) TD-2: `package.json` の rollup ネイティブバイナリ・ハードコード除去 → lockfile 再生成（--force なし）→ `npm audit fix`
  - (d) TD-3: `src/test/factories/` 共通基盤を新設し既存テストの重複 factory を集約
  - (e) TD-5: 再設計後も生き残る確定資産（masteryManager / equipmentStats / buffCalculation）の純関数テスト追加
  - (f) BAT-13 再スコープ版: 現行戦闘の最小スモーク統合テスト1〜2本（初期化→カード1枚実行→敵フェーズ→決着の通し）。網羅的な現行フロー回帰テストは書かない（TD-6 negative list）
  - (g) TD-6: テスト投資の線引き（書く＝生存資産・スモーク / 書かない＝置換予定の肥大フック）を `docs/code-explanation/testing_analysis.md` に明文化
  - (h) TD-7: 聖域 `enhancedElements` 未結線を結線（TD-1 と同一コミット推奨）。結線しない場合は known-issue 起票 + R1-15 差分ゼロ監査の除外リストへ登録
- **依存**: なし（最優先）
- **受け入れ基準**:
  - [ ] darwin で `npm install`（--force なし）/ `npm audit fix` が成功
  - [ ] known-issue 001 が Fixed になり回帰テストがある
  - [ ] BAT-13 スモークが現行戦闘の通しを green で保証
  - [ ] テスト線引き文書（TD-6）が存在し、MEMORY.md の負債項目（001 / rollup / テスト）がクローズ
  - [ ] `npm run build` / `test:run` / lint が全通過

## R1-1: vision 正本の一本化 + 要件 Tier 確定（DOC-1 / DOC-25）

- **由来**: concept §7 step 2 / gap-analysis §5 Phase 1 先頭 / DOC-1, DOC-24, DOC-25
- **内容**: `concept-v2.md` を `docs/vision/core.md` へ統合し正本を一本化（旧 Core Value = 5深度×5フロア・StS 系の記述を置換）。kickoff 資料は `archive/` へ。本 requirements/ 群を正式版に確定。`docs/INDEX.md` を新構成へ更新（新規設計書の置き場・凍結文書の Status 凡例を含む）
- **依存**: R1-0
- **受け入れ基準**:
  - [ ] `core.md` に concept-v2 と矛盾する旧構成記述が残っていない
  - [ ] INDEX に新規4設計書（戦闘コア / ステージ・環境 / 観察・手記 / 演出）の置き場が定義されている

## R1-2: 新戦闘コア設計書（実装に先行・DOC-2/3/4）

- **由来**: concept §1・§5 合格基準(1) / BAT-1, BAT-7, BAT-10, DOC-2, DOC-3, DOC-4
- **内容**: `docs/battle_document/` を全面改訂。(a) 間合い（距離段階・初期距離・移動コスト・最適/不適補正）(b) 体勢（ゲージ or 段階・崩し条件・崩れペナルティ・既存 stagger との統合）(c) スタミナ（最大値・消費表・回復則の有無と量・戦闘終了リセット）(d) 剣気の役割再定義（BAT-7: スタミナ＝消耗側 / 剣気＝攻勢蓄積側の重複なき分担、崩し成立時の剣気ボーナス等の相互作用）(e) バフ・デバフ42種の新3要素との干渉棚卸しと維持/再定義/凍結の確定（BAT-10 → buff_debuff_system.md Ver 6.0 = DOC-4）。battle_logic.md は Ver 5.0、エネルギー廃止を反映。**全 R1-3〜R1-9 のスキーマの正となるため戦闘実装より先に確定必須**
- **依存**: R1-1
- **受け入れ基準**:
  - [ ] 間合い・体勢・スタミナの全数値（剣士カード60枚分のコスト表含む）が記載
  - [ ] エネルギー廃止が反映され旧エネルギー記述が残っていない
  - [ ] 剣気とスタミナの獲得・消費条件が分離して定義されている
  - [ ] 全42バフ・デバフが維持/再定義/凍結の棚卸し表に分類済み

## R1-2.5: 戦闘オーケストレーター合成境界整理（BAT-12）

- **由来**: gap-analysis §4 修正#11（M→L 格上げ・スモーク前提）/ §5 Phase 2 で BAT-11 直後の必須前段 / BAT-12
- **内容**: 1006行の `useBattleOrchestrator` の合成境界を整理し、新3システム用フックの差し込み口を用意する。肥大フックへ直接増築する手戻りを防ぐ土台工程。R1-0(f) のスモークテストを安全網とする
- **依存**: R1-0（スモーク）, R1-8（魔術師隔離 = BAT-11）
- **受け入れ基準**:
  - [ ] スモークテストが green のまま境界整理が完了
  - [ ] 新システム（スタミナ/間合い/体勢）の hook を差し込む口が用意されている

## R1-3: 間合いシステム（BAT-2/3/4）

- **由来**: concept §1 主柱・体験の柱1 / BAT-2, BAT-3, BAT-4
- **内容**: プレイヤー×敵ごとの距離状態（段階制。段数は R1-2 設計書で確定、モック想定は遠/中/近）。カード型に有効間合いと距離変更効果を追加し、`canPlayCard` / 効果プレビュー / ダメージ計算（最適間合いボーナス・不適ペナルティ）に統合。状態は EnemyBattleState 側に持たせ単一所有を守る。移動手段カードの新設は R1-7（CRD-4）で行う
- **依存**: R1-2, R1-2.5
- **受け入れ基準**:
  - [ ] 間合い外のカードはプレイ不可（グレーアウト + 理由表示、R1-6 連動）
  - [ ] 移動カードで距離が変わり、敵の距離変更行動も機能する
  - [ ] 複数敵戦で敵ごとの間合いが独立管理される

## R1-4: 体勢システム（BAT-5 + BAT-10 干渉整理）

- **由来**: concept §1 主柱 / BAT-5, BAT-10
- **内容**: 体勢値（プレイヤー・敵双方）。大技・移動・被弾で増減し、尽きると「崩れ」（被ダメ増・行動制限）。既存 stagger デバフ・`canAct` 判定と統合し行動不能の二重判定を作らない。「崩してから叩く」の駆け引きを成立させる。R1-2 のバフ棚卸し（BAT-10）に従い slow/stall↔スタミナ、momentum/focus 等の干渉を実装
- **依存**: R1-2, R1-2.5
- **受け入れ基準**:
  - [ ] 自分の大技連発で体勢が崩れるリスクが発生する
  - [ ] **崩れ状態の敵への攻撃に、設計書定義の被ダメ増補正・行動制限が適用されることを state/ログで確認できる**
  - [ ] stagger との関係が設計書どおりで行動不能判定が一本化されている

## R1-5: 敵側の新3要素対応（行動スキーマ + AI）

- **由来**: concept §1（敵も同一ルール）/ BAT-9, ENM-1, ENM-2, ENM-9, DOC-13
- **内容**: ステージ別敵 DB 設計書（DOC-13 相当）を実装に先行作成。`EnemyAction` に間合い要件・移動行動・スタミナ/体勢への作用を追加。敵フェーズ実行で距離判定・距離変更を処理。enemyAI が間合いを考慮。対象母集合は **2ステージに配属する敵（既存40体から選抜・再配分）+ 試験敵5体**。未配属の敵データは凍結注記付きで保持（削除しない）
- **依存**: R1-2, R1-3, R1-4
- **受け入れ基準**:
  - [ ] 敵が間合い外から攻撃せず、詰める/離す行動を取る
  - [ ] 2ステージ配属敵 + 試験敵5体が新スキーマで定義され、新コード経路（ステージ敵プール）から旧 aiPatterns が参照されない
  - [ ] 未配属の敵データに凍結注記がある

## R1-6: DOM 戦闘 HUD（新3要素の常時可視化・案B まで）

- **由来**: 体験の柱1（読めて初めて成立）/ X-3, DOC-23 / モックアップ `docs/vision/mockups/2026-06-11-battle-ui-mockup.html`（UI 構成の**先行資産**。確定仕様は ui_ux_design_guide.md 改訂 = DOC-23 を正とする）
- **内容**: PlayerFrame にスタミナゲージ・体勢表示、間合い表示（**案B バッジ型まで**。案A トラック型は R2-3 の Pixi レーン表示で実現）、手札のプレイ不可フィードバック（間合い外/スタミナ不足の理由表示）、ステージヘッダー（環境チップ・推奨等級）
- **依存**: R1-3, R1-4（および R1-2 スタミナ）
- **受け入れ基準**:
  - [ ] スタミナ・体勢・現在間合いが戦闘中常時読める
  - [ ] プレイ不可カードに不可理由が表示される
  - [ ] CSS は `.battle-screen` スコープ規約を守る

## R1-7: 剣士カード全60枚の再定義（CRD-1/3/4/5/8）

- **由来**: concept §1「カード＝実際に取れる行動」/ CRD-1, CRD-3, CRD-4, CRD-5, CRD-8, DOC-6
- **内容**:
  - 剣士カード全60枚（基本37 + タレント8 + 派生15）にスタミナコスト・有効間合い・体勢影響を定義（数値は R1-2 設計書）。CardComponent に新3属性を表示
  - CRD-4: 移動・体勢立て直しカードの新設（CardTag 追加と UI 波及含む）。距離を変える手段が無いと柱1「間合いの読み合い」が成立しないため必須
  - CRD-5: 初期デッキ15枚を「間合いを取る→隙を作る→重い一撃」の基本ループが成立する構成へ再編し、**新コアの最初の通し検証単位**とする
  - CRD-8: 熟練度→派生の**レジストリ構造と ID 体系は keep**（CRD-6/7）。熟練度ボーナスの意味（Lv4 で 2.5x の単純ダメージ倍率は「一手のミスが命取り」のコアファンタジーと衝突し得る）はスタミナ効率・体勢崩れにくさ等の技術表現へ再定義を検討（数値は R1-2 設計書で確定。`MASTERY_BONUSES` の export は shim 互換のため維持し中身のみ変更）
  - 不可侵 `deck.ts` / `deckReducer.ts` には optional フィールド追加方式で抵触しない（制約整合クリティーク確認済み）
- **依存**: R1-2, R1-3, R1-4
- **受け入れ基準**:
  - [ ] 派生・タレント含む全60枚が新3属性を持ち設計書の数値表と一致
  - [ ] 初期デッキ15枚のみで基本ループ（間合い→隙→一撃）が成立する
  - [ ] 派生カード・talentCardRegistry の整合が保たれている

## R1-8: 魔術師の一時無効化（BAT-11）

- **由来**: concept §4 剣士先行 / X-1, BAT-11 / **§6 決定#2: 選択不可にする**
- **内容**: `CharacterClassData` の魔術師を `isAvailable: false` + キャラ選択画面に「調整中」表示。既存魔術師セーブの Continue にはロード不可の注記。useElementalChain 等の魔術師コードは隔離（削除しない）。element_system_spec に剣士先行の注記（DOC-5）
- **依存**: R1-0
- **受け入れ基準**:
  - [ ] 魔術師が選択できず理由が表示される
  - [ ] 魔術師セーブで Continue してもクラッシュせず案内が出る
  - [ ] 剣士のプレイに魔術師コードが影響しない

## R1-9: ステージ制への移行（DUN-1/3/6 + 周辺データ）

- **由来**: concept §3 B1/B3・ステージ構成 / DUN-1, DUN-3, DUN-6, DUN-11, CAMP-5, CAMP-11, JNL-7, X-6 / **§6 決定#4: Difficulty 廃止** / **#6: Depth 段階移行→排除**
- **内容**: ステージ定義・環境ルール仕様書（DOC-9/10 相当）を実装に先行作成。`StageDefinition` 型を新設（テーマ・階層数・敵プール参照・推奨等級・環境ルール・初期間合い・ショップ在庫テーブル）。開発時は2ステージ。ステージ選択画面（どこからでも入れる・推奨等級表示）が難易度選択を兼ねる。推奨等級は StageDefinition を**単一データソース**にギルド掲示板（CAMP-5）と選択画面（DUN-6）で表示コンポーネントを共有（修正#15）。`Difficulty` 型廃止・ライフ数固定。`Depth` はマッピング層で段階移行し union 完全削除は DUN-3 完了条件。ショップ在庫（CAMP-11）・NodeMap フロア表示/クリア判定（DUN-11）・図鑑分類軸（JNL-7）の Depth 依存をステージ化。**Tier 1 で戦闘へ反映する環境効果は初期間合いのみ**（視界・足場・広さの戦闘反映と DOC-10 数値確定は R2-2）
- **依存**: R1-2（環境ルールが戦闘仕様を参照）
- **受け入れ基準**:
  - [ ] 2ステージがステージ選択画面から開始でき等級が表示される
  - [ ] 敵プール・報酬・ショップ在庫・図鑑分類を含む Depth 依存データがステージ定義から引かれる
  - [ ] `LIVES_BY_DIFFICULTY` と Difficulty 選択経路が排除されている

## R1-10: 観察システム（敵情報の段階開示）

- **由来**: concept §1 副柱「情報」/ JNL-1, JNL-2, JNL-8（ENM-3/4/5 統合。オーナー = journal-knowledge、状態所有 = JournalContext）/ DOC-15
- **内容**: 観察・手記仕様書（DOC-15 相当）を実装に先行作成。敵種ごとの観察レベル（☆☆☆→★★★ の4段階、モック③）。初見は名前のみ、観察行動・遭遇回数で HP 手応え→意図→数値・弱点と開示。EnemyFrame は観察レベルに応じマスキング。観察状態の所有は JournalContext 一本
- **依存**: R1-2.5, **観察・手記仕様書（DOC-15）の先行作成**, R1-11
- **受け入れ基準**:
  - [ ] 初見の敵は HP / 意図 / 弱点が見えない
  - [ ] 観察レベルが上がると段階的に開示される
  - [ ] 観察状態の書き込み箇所が JournalContext のみ（単一所有監査）

## R1-11: 手記の死越え永続（JNL-5）

- **由来**: concept §3 A1 / JNL-5（IRR-5 統合）/ **§6 決定#3: フルリセットでも全て残す**
- **内容**: 観察レベル・図鑑・手記の書き込みはラン死亡でもゲームオーバー（ライフ0）でも消えない唯一の資産。明示的なデータ削除のみで消える、と定義（DOC-16 へ反映）
- **依存**: なし（仕様確定が R1-10 / R1-14 の入力。序盤で並行確定）
- **受け入れ基準**:
  - [ ] 死亡 → 手記の内容が保持されている
  - [ ] ライフ0 フルリセット → 手記の内容が保持されている

## R1-12: 回復経済の再設計（A2 / CAMP-1/2/9/10）

- **由来**: concept §3 A2「魔法は希少・回復は高価」/ CAMP-1, CAMP-2, CAMP-9, CAMP-10, DOC-19
- **内容**: 回復経済設計書（DOC-19 相当）を改訂。ダンジョン帰還時の無償フルヒールを廃止し宿（サンクチュアリ）の有料回復へ。ポーション価格・回復量の再設定。rest ノード回復量見直し。CAMP-9: Sanctuary「Boon of Recovery」死に効果を宿の恒久強化として再定義 or 削除（CAMP-1 確定後）
- **依存**: R1-2, R1-16（生還時の探索資源確定が無いと支払い不能）
- **受け入れ基準**:
  - [ ] 無償フルヒール経路が存在しない
  - [ ] 回復価格が設計書と一致し通しプレイで経済が破綻しない（合格基準2）
  - [ ] boon_recovery の処遇が決定・反映されている

## R1-13: 昇格試験の新コア適合（X-2）

- **由来**: B3 等級の前提 / X-2 / **§6 決定#5: 新コア適合**
- **内容**: GuildBattleScreen の depth=1 固定呼び出しをステージ制 API へ追随。試験敵5体（GuildEnemyData）に新3要素属性を付与（R1-5 母集合に含む）。試験用環境（闘技場）を StageDefinition or BattleEnvironment として定義
- **依存**: R1-5, R1-9
- **受け入れ基準**:
  - [ ] 昇格試験が新コアで開始〜決着まで動く
  - [ ] 昇格等級がステージ推奨等級（B3）と対応している

## R1-16: 不可逆性の確定処理（副柱2 / IRR-1/3/4/6/8/12）

- **由来**: concept §1 優先2 副柱「結果は不可逆に残る」/ IRR-1, IRR-3, IRR-4, IRR-6, IRR-8, IRR-12, X-8
- **内容**: 死亡・生還・ゲームオーバーを各1つの確定トランザクション化:
  - IRR-6: 生還時の souls 移管 + 探索資源（ゴールド/ソウル）の拠点移動 + 自動セーブを1トランザクションに（現状 transferSouls / completeSurvival / transferExplorationToBaseCamp の呼び出しがゼロ。これが無いと R1-12 の有料回復が支払い不能）
  - IRR-3: 死亡確定処理の一本化 + 即時保存
  - IRR-4: ライフ0 フルリセットの全コンテキスト初期化 + ゲームオーバー画面（手記は R1-11 仕様で残す）
  - IRR-1: 確定イベント駆動の自動セーブ（セーブスカム防止。現状リロードでペナルティが巻き戻る）
  - IRR-8: cardTypeId ベースのデッキ・解放状態（unlockedCardTypeIds・熟練度）の保存復元（ロードのたびに購入/派生カードが消える＝柱3「成長の手応え」の毀損を解消）
  - IRR-12: ラン中断＝放棄の仕様化
- **依存**: R1-9（ステージ確定後）。R1-14 の入力
- **受け入れ基準**:
  - [ ] 生還 → 拠点で探索ゴールド/ソウルが確定加算されている
  - [ ] 死亡 → リロードしてもペナルティが巻き戻らない
  - [ ] ロード後も購入/派生カードと熟練度がデッキに残る

## R1-14: SaveData v2.0（IRR-7/2/8）

- **由来**: IRR-7（影響に `CharacterSelect.tsx:91-133` の初期セーブ生成を含む）/ IRR-2, IRR-8
- **内容**: 新状態（ステージ進行・観察レベル・手記・スタミナ関連設定・Difficulty 廃止・ライフ[IRR-2]・cardTypeId ベースのデッキ/解放/熟練度[IRR-8]）を反映したセーブスキーマ v2.0 と旧セーブからのマイグレーション。**R1-8〜R1-11 と R1-16 の状態形が確定してから1回で設計**（クリティカルパス Phase 4）
- **依存**: R1-8, R1-9, R1-10, R1-11, R1-16
- **受け入れ基準**:
  - [ ] v1 セーブが v2 へマイグレーション or ロード不可案内（魔術師セーブは R1-8 仕様）
  - [ ] セーブ→ロードで新状態（観察・ステージ進行・ライフ・デッキ）が完全復元
  - [ ] ロード後に購入・派生カードと熟練度が失われない / ライフ数がセーブを跨いで保持

## R1-17: 新コアの学習手段（最小版・X-4）

- **由来**: 合格基準2「詰みなし」の前提 / X-4, CAMP-6, DOC-21 / クリティーク（Tier 2 では合格基準が通らない）
- **内容**: GAME_TIPS 全面書き直し（旧コア解説の英語 tips → 間合い・体勢・スタミナ・観察の4ルールを日本語で。熟練度の誤記も解消）+ 初回プレイ導入の要否確定（チュートリアル戦闘の有無）。game_design_master（DOC-21）を新コアへ。**拡充版（チュートリアル戦闘本体など）は R2-4 に残す**
- **依存**: R1-2〜R1-7（ルール確定後）。実施は R1-15 監査の前
- **受け入れ基準**:
  - [ ] 新ルール4つすべてにゲーム内の説明手段がある
  - [ ] tips の内容が実装数値と一致（差分ゼロ監査対象）

## R1-18: 帰還システム（前半・DUN-10）

- **由来**: concept §1 副柱「結果は不可逆に残る」/ DUN-10, DOC-12（合格基準1: 現状テレポートストーン0固定 + TODO のプレースホルダで設計書と乖離）
- **内容**: return_system_design.md（DOC-12）をステージ制へ改訂。テレポートストーン消費 + 帰還確定処理（旧「深度5帰還禁止」のステージ属性化）。IRR-6 の生還確定と接続。**帰還ルート（遭遇率逓減の逆走）は Tier 2（R2-8）へ分離**。実装しない場合は設計書側を縮小改訂して差分ゼロにする
- **依存**: R1-9, R1-16
- **受け入れ基準**:
  - [ ] テレポートストーン消費で帰還が確定し、生還処理（R1-16）に接続される
  - [ ] return_system_design.md と実装の差分がゼロ

## R1-15: 合格基準の監査手段（X-7）

- **由来**: concept §5 / X-7, DOC-11/14/22/24
- **内容**: (a) 設計書ごとの「差分ゼロ監査チェック表」（card-battle-balance-auditor の観点表を兼ね、TD-1/TD-7 の除外リストを含む）(b) 2ステージ通しプレイのチェックリスト（クラッシュ・詰み・バランス崩壊の判定基準）。監査母集合の整備として DOC-11/14/22/24 の改訂・凍結アーカイブ移動を含む
- **依存**: R1-2（設計書群の構成確定後）、実施は全 Tier 1 完了時
- **受け入れ基準**:
  - [ ] 全 Tier 1 要件に対応する監査項目が存在
  - [ ] 通しプレイチェックリストが pass/fail で判定可能な粒度

---

## Tier 1 実施順序（gap-analysis §5 クリティカルパス準拠）

```
Phase 0   R1-0（負債返済。TD-1 含む）
Phase 1   R1-1（vision 正本一本化）→ R1-2（新戦闘コア設計書 = 以降の正）
          ※ R1-8 魔術師無効化 / R1-11 手記仕様 は序盤で並行確定可
Phase 2   R1-8 →R1-2.5（合成境界整理）→ R1-3 / R1-4 → R1-5 / R1-6 / R1-7
Phase 3   R1-9（ステージ）→ R1-10 / R1-12 / R1-13 / R1-18
          ※ R1-16 不可逆性確定処理は R1-9 後・R1-14 前
Phase 4   R1-16 → R1-14（SaveData v2.0。状態形が出揃ってから1回で）
Phase 5   R1-17（学習手段）→ R1-15（監査実施）
```
