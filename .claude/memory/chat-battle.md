# MEMORY (chat-battle)

> 進行中 / 直近の完了 / 予定の正本。**手動編集せず task-tracker スキル経由で更新**。完了タスクの詳細は `history/chat-battle.md`、要約は `README.md` の Development History。
> レーン `battle` = 戦闘プログラム（`unity-port/BattleCore/` が正）と戦闘設計書。既定の `area:` は `battle` `unity`。

## 進行中

（なし）

## 直近の完了

- BattleCore に v4.2 の最小の骨を入れる（#69）✅（2026-09-21）— v3 を置き換えた。固定コスト（列 = コスト）/ 近間・遠間の 2 値 / 手札 5 枚 / `(面 + 特性) → AwayFromZero → − Guard` / 特性の評価器（条件 4 語 × 効果 3 語）/ 鈍足のスタック。ターン進行・敵データ・View 契約（`BattleStore` / `IBattleView` / `ViewModel`）は v3 ごと外し、#70 / #71 / #72 が建て直す。`ParityTests.cs` も削除（v3 以降 `V2_PARITY` 付きの死んだコード）。PR #133（open、merge は人の手番）。確認: `npm run build` / `npm run lint` / `npm run test:run` / `dotnet test` がすべて exit 0。BattleCore.Tests 54 → 79 件、Depiction 53 件は不変
- 縦切りの規則の仕分けと合格条件（#68）✅（2026-09-21）— `battle_core_v4.md` の規則を R01〜R111 に番号付けし、使う 72 / 使わない 35 / 仮 4 に分けた。計画書は `docs/vision/plans/2026-09-21-vertical-slice-polearm.md`（#69〜#80 が行番号で範囲を指す）。1 ターンの合格条件を並び 17 件 + 数値 8 点 + 終端 3 点で定義。正本の食い違い 2 件は直さず #116 へ。main にマージ済み（428e66d）
- 投入量の代わりのシナジー設計（#62）✅（2026-09-21）— `battle_core_v4.md` に §19 を追加し、Issue #62 の 6 問に結論を出した。新しい軸は足さず、いまの 5 軸を「時間の 4 層」（1 枚 / 1 ターン / 2 ターンの往復 / 1 戦と 1 つの生）に並べ直し、名前の無かった層 2 と層 3 に規則 S1〜S17 を置いた。強い組み合わせ 6 型（溜め放ち / 振り子 / 研ぎ / 血路 / 一型 / 背水）に止め役を付け、§13 に基準 10〜14 を追加。カード 1 枚ずつの数値・状態の語 10・スタンスの型 6・`src/` には触っていない。PR #63（open、merge は人の手番）。確認: `cd unity-port && dotnet test` が 107 合格・0 失敗

## 予定

縦切り（#67）を #70 → #71 → #72 → #73 → #74 の順で進める。#70 と #71 は互いに独立で、どちらも #69 の型の上に乗る。

- 🔜 **#70 長柄の歪み兵を C# のデータにする** — `enemy_roster_v4.md` §2.1 の 4 行動・2 枝の決定木・予兆 1 段を `EnemyDef` / `EnemyActionDef` で書く。特性 3 つ（位置(相手 遠間) / 無防備 / 温存）は `Traits` の語彙で足りる。id `polearm_warped` と表示名は変えない（改名は #128）。予兆の一字は roster の表の値をそのまま出す（#116 の 2 件目）
- 🔜 **#71 試作用デッキ 10 種を選ぶ** — `swordsman_cards_v4.md` から 10 種 × 2 = 20 枚。§3.1 の目盛り（`Columns` にある）から数値を取る。切り替えの札が 5 枚に 1 枚も来ない確率を目視する
- 🔜 **#72 ターン進行を通す** — 計画書の Verification がそのまま Definition of Done（並び 17 件 + 数値 8 点 + 終端 3 点）。イベント列の型は #72 が決める（#69 では入れなかった）
- 🔜 **§19.9 の本文改訂を回す** — §19 で決めた S1〜S17 を本文へ落とす。`battle_core_v4.md` の §2.3（手薄の定義）/ §17.6 F4（倍率 1 つ）/ §17.8（型替えの閾値 2 / 6 / 12）/ §8・§11（スタンス 3 枚制限）。`swordsman_cards_v4.md` の 60 種への 伸び / 軽さ / 型替え 割り当ては、列（= コスト）の割り当て（§18.3）と同じ回でやる。`enemy_roster_v4.md` は 9 体に「近間を咎める技 / 遠間を咎める技」を各 1 つ以上（S11）
- 🔜 **試験台（#53）で基準 10〜14 を測る** — §19 の数値は全て叩き台。測る順は 10（3 枚以上出したターンが 2 回以上）→ 12（1 撃の最大が 42 以下）→ 11（間合いを振った側が損をしない）→ 13 / 14。10 が 0 回なら回復 3 か手薄の定義を見直す
