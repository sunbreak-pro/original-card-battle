{
  "name": "card-battle-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Workspace root for card battle game",
  "scripts": {
    "dev": "cd card-battle-main && npm run dev",
    "build": "cd card-battle-main && npm run build",
    "lint": "cd card-battle-main && npm run lint",
    "preview": "cd card-battle-main && npm run preview",
    "install-deps": "cd card-battle-main && npm install"
  },
  "workspaces": [
    "card-battle-main"
  ]
}
